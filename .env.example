NODE_ENV=development
PORT=5174

# Railway Postgres (real value will be set in Railway → Variables)
DATABASE_URL=postgresql://user:pass@host:5432/dbname

JWT_SECRET=change_me_in_production

# Force REST (no Supabase)
VITE_API_MODE=api
# Keep empty in production so same-origin '/api' is used
VITE_API_BASE_URL=